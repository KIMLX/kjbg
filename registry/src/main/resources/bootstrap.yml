spring:
  application:
    name: registry
  cloud:
    config:
      uri: http://localhost:9099
      fail-fast: true
      password: lxj3889
      username: user

eureka:
  instance:
    prefer-ip-address: true
  client:
    registerWithEureka: false
    fetchRegistry: false
    server:
      waitTimeInMsWhenSyncEmpty: 0